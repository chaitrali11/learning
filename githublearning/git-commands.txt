#git --version
#git init
#git config --global user.name “your name”
#git config --global user.email “your email id”
#git config --list
#git help config
#git config --help
#git status
#git add <file/directory name>   : to push the directory/file in staging area.
#git reset <file/directory name> : to remove file/directory from staging area.
#git reset  : will remove all files/directories from staging area.
#git add -A : will push all untracked file/directories in staging area.
#git commit -m “enter message why you are changing in repos” : it will save save your changes in your local repos.
#git log  : it will show the logs.
#git clone <url> <path where you want to clone>
#git remote -v
#git remote add <new repo name> <url>  : it will create the new repository .
#git fetch all
#git remote show all
#git branch -a : it will show you list of all branches
#git branch Branch1 : It will create new branch with name Branch1
#git checkout Branch1 : it will change the branch as Branch1.
#git push -u <repo name> Branch1 
#git checkout master   : to change the branch as master
#git pull <repo name> master
#git pull <user name>  : To pull the new updates from remote repos
#git branch merge
#git merge Branch1  : To merge the branch Branch1 data to current branch.
#git push <repo name> master : push the change to remote repositories on master branch	. 
#git branch --merged
#git branch -D Branch1 : To delete the branch locally 
#git push <repo name>  --delete Branch1 : To delete the branch remote server.
#alias graph='git log --all --decorate --oneline --graph'
